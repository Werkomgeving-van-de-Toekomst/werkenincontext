---
phase: 2.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/iou-frontend/src/state/url_state.rs
  - crates/iou-frontend/src/state/mod.rs
  - crates/iou-frontend/src/components/url_state.rs
  - crates/iou-frontend/src/components/mod.rs
  - crates/iou-frontend/src/pages/data_verkenner.rs
  - crates/iou-frontend/src/components/filter_panel_3d.rs
  - crates/iou-frontend/src/components/view_toggle.rs
  - crates/iou-frontend/src/components/density_heatmap.rs
autonomous: true
requirements:
  - POLI-01
user_setup: []

must_haves:
  truths:
    - "User can share URL and recipient sees same filter/view/heatmap state"
    - "URL contains readable query parameters (not opaque base64)"
    - "Browser back button restores previous state"
    - "URL updates when filters change"
    - "URL updates when view mode toggles"
    - "URL updates when heatmap toggles"
  artifacts:
    - path: "crates/iou-frontend/src/state/url_state.rs"
      provides: "UrlState struct with serialization to/from query params"
      min_lines: 80
      contains: "to_url_params", "from_url_params", "impl Serialize", "impl Deserialize"
    - path: "crates/iou-frontend/src/components/url_state.rs"
      provides: "URL state manager component with JS interop"
      min_lines: 150
      exports: ["build_update_url_script", "build_restore_state_script", "UrlStateManager"]
    - path: "crates/iou-frontend/src/pages/data_verkenner.rs"
      provides: "URL state initialization on mount"
      contains: "use_effect for URL restore"
  key_links:
    - from: "crates/iou-frontend/src/components/url_state.rs"
      to: "window.history.replaceState()"
      via: "document::eval()"
      pattern: "replaceState.*searchParams"
    - from: "crates/iou-frontend/src/pages/data_verkenner.rs"
      to: "URL state components"
      via: "use_effect on mount"
      pattern: "use_effect.*restore.*url"
    - from: "FilterPanel3D/ViewToggle/DensityHeatmap"
      to: "URL state manager"
      via: "state change callbacks"
      pattern: "onchange.*update_url"
---

<objective>
Implement URL query parameter persistence for filter, view, and heatmap state enabling shareable URLs and browser history navigation.

Purpose: Users can share their current map view (filters + 2D/3D mode + heatmap) with others via URL, and browser back/forward buttons restore previous states.

Output: Working URL state sync where state changes update query params (`?view=3d&year_min=1950&year_max=2024...`) and page load restores from URL.
</objective>

<execution_context>
@/Users/marc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marc/.claude/get-shit-done/templates/summary.md

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/2.4-polish/2.4-CONTEXT.md
@.planning/phases/2.4-polish/2.4-RESEARCH.md

@crates/iou-frontend/src/state/building_filter.rs
@crates/iou-frontend/src/state/view_toggle.rs
@crates/iou-frontend/src/state/density_heatmap.rs
@crates/iou-frontend/src/components/view_toggle.rs
@crates/iou-frontend/src/components/filter_panel_3d.rs
@crates/iou-frontend/src/components/density_heatmap.rs
</context>

<interfaces>
<!-- Existing state structs that already implement Serialize/Deserialize -->

From crates/iou-frontend/src/state/building_filter.rs:
```rust
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct BuildingFilter {
    pub min_year: u32,
    pub max_year: u32,
    pub min_height: f64,
    pub max_height: f64,
    pub min_floors: u32,
    pub max_floors: u32,
}
```

From crates/iou-frontend/src/state/view_toggle.rs:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum ViewMode {
    TwoD,
    ThreeD,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct ViewToggle {
    pub mode: ViewMode,
}
```

From crates/iou-frontend/src/state/density_heatmap.rs:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]
pub struct DensityHeatmap {
    pub enabled: bool,
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create UrlState module for query parameter serialization</name>
  <files>crates/iou-frontend/src/state/url_state.rs</files>
  <behavior>
    - Test 1: UrlState serializes to readable query params (view=3d, year_min=1900, year_max=2024, height_min=0, height_max=100, floors_min=1, floors_max=10, heatmap=false)
    - Test 2: UrlState deserializes from query params correctly
    - Test 3: UrlState::default() returns sensible defaults (3D view, no filters, heatmap off)
    - Test 4: Roundtrip serialization preserves all values
    - Test 5: Missing query params use defaults (partial state restoration)
  </behavior>
  <action>
    Create `crates/iou-frontend/src/state/url_state.rs` with UrlState struct that:
    - Combines ViewMode, BuildingFilter, and DensityHeatmap into a single struct
    - Implements to_url_params() for readable query string (not base64)
    - Implements from_url_params() for parsing query string into state
    - Uses Default trait for sensible defaults
    - Includes 6 unit tests covering serialization/deserialization

    Follow existing state module patterns from building_filter.rs, view_toggle.rs, density_heatmap.rs
  </action>
  <verify>
    <automated>cargo test -p iou-frontend url_state</automated>
  </verify>
  <done>UrlState module exists with to_url_params/from_url_params methods, all tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export UrlState from state module</name>
  <files>crates/iou-frontend/src/state/mod.rs</files>
  <behavior>
    - Test 1: UrlState is exported from state module
    - Test 2: Can import UrlState via crate::state::UrlState
  </behavior>
  <action>
    Add to `crates/iou-frontend/src/state/mod.rs`:
    ```rust
    pub mod url_state;
    pub use url_state::UrlState;
    ```
    Place after the density_heatmap module export (line 10).
  </action>
  <verify>
    <automated>cargo test -p iou-frontend state</automated>
  </verify>
  <done>UrlState is exported from state module, compiles successfully</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create URL state manager component with JS interop</name>
  <files>crates/iou-frontend/src/components/url_state.rs</files>
  <behavior>
    - Test 1: build_update_url_script generates JS that calls history.replaceState()
    - Test 2: build_update_url_script sets correct query params from UrlState
    - Test 3: build_restore_state_script reads from window.location.search
    - Test 4: build_restore_state_script writes to global window['initialUrlState']
    - Test 5: build_restore_state_script handles empty query string
  </behavior>
  <action>
    Create `crates/iou-frontend/src/components/url_state.rs` with:
    - build_update_url_script(state: &UrlState) -> String - updates URL via history.replaceState()
    - build_restore_state_script() -> String - reads window.location.search on mount
    - build_get_url_params_script() -> String - gets current params as JSON
    - 5 unit tests verifying script content

    Follow JS interop pattern from view_toggle.rs (document::eval, localStorage access)
  </action>
  <verify>
    <automated>cargo test -p iou-frontend url_state</automated>
  </verify>
  <done>URL state manager component exists with JS interop functions, all tests passing</done>
</task>

<task type="auto">
  <name>Task 4: Export URL state component and integrate into DataVerkenner</name>
  <files>crates/iou-frontend/src/components/mod.rs, crates/iou-frontend/src/pages/data_verkenner.rs</files>
  <action>
    1. Add to `crates/iou-frontend/src/components/mod.rs`:
       ```rust
       mod url_state;
       pub use url_state::{build_update_url_script, build_restore_state_script, build_get_url_params_script};
       ```

    2. In `crates/iou-frontend/src/pages/data_verkenner.rs`:
       - Import URL state functions
       - Add use_effect hook after Map3D initialization to restore URL state on mount
       - Components will read from URL params via their own hooks
  </action>
  <verify>
    <automated>cargo check -p iou-frontend</automated>
  </verify>
  <done>URL state component exported and DataVerkenner initialized with URL restore on mount</done>
</task>

<task type="auto">
  <name>Task 5: Integrate URL sync into FilterPanel3D component</name>
  <files>crates/iou-frontend/src/components/filter_panel_3d.rs</files>
  <action>
    Modify `crates/iou-frontend/src/components/filter_panel_3d.rs`:
    - Import URL state functions and UrlState
    - Update existing filter change use_effect to also update URL
    - Build UrlState with current filter and call build_update_url_script()
    - Execute URL update script after filter update script
  </action>
  <verify>
    <automated>cargo test -p iou-frontend filter_panel_3d</automated>
  </verify>
  <done>FilterPanel3D updates URL when filters change, imports added</done>
</task>

<task type="auto">
  <name>Task 6: Integrate URL sync into ViewToggle component</name>
  <files>crates/iou-frontend/src/components/view_toggle.rs</files>
  <action>
    Modify `crates/iou-frontend/src/components/view_toggle.rs`:
    - Import URL state functions and UrlState
    - Update onclick handler to update URL after view mode change
    - Build UrlState with current mode and call build_update_url_script()
    - Execute both view change script and URL update script
  </action>
  <verify>
    <automated>cargo test -p iou-frontend view_toggle</automated>
  </verify>
  <done>ViewToggle updates URL when mode changes</done>
</task>

<task type="auto">
  <name>Task 7: Integrate URL sync into DensityHeatmap component</name>
  <files>crates/iou-frontend/src/components/density_heatmap.rs</files>
  <action>
    Modify `crates/iou-frontend/src/components/density_heatmap.rs`:
    - Import URL state functions and UrlState
    - Update onclick handler to update URL after heatmap toggle
    - Build UrlState with current heatmap state and call build_update_url_script()
    - Execute URL update script after heatmap toggle script
  </action>
  <verify>
    <automated>cargo test -p iou-frontend density_heatmap</automated>
  </verify>
  <done>DensityHeatmap updates URL when toggled</done>
</task>

</tasks>

<verification>
After completion, verify in browser:
1. Load page at root URL - should see default state
2. Change filters - URL should update with year_min/year_max/height_min/height_max/floors_min/floors_max
3. Toggle 2D/3D - URL should update with view=2d or view=3d
4. Toggle heatmap - URL should update with heatmap=true or false
5. Copy URL with state, open in new browser - should restore state
6. Use browser back button - should restore previous state
</verification>

<success_criteria>
- URL updates when any state changes (filter, view, heatmap)
- URL parameters are human-readable (not base64)
- Opening a URL with query params restores the corresponding state
- Browser back/forward buttons work with URL state
- All unit tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/2.4-polish/2.4-01-SUMMARY.md`
</output>
