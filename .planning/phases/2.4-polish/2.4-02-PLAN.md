---
phase: 2.4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/iou-frontend/assets/style.css
  - crates/iou-frontend/src/components/view_toggle.rs
  - crates/iou-frontend/src/components/filter_panel_3d.rs
  - crates/iou-frontend/src/components/density_heatmap.rs
autonomous: false
requirements:
  - POLI-02
  - POLI-03
user_setup: []

must_haves:
  truths:
    - "2D/3D toggle animates smoothly (500ms camera + 300ms UI fade)"
    - "Filter changes don't cause UI jank"
    - "All control buttons use CSS variables from style.css"
    - "No hardcoded colors in component Rust code"
    - "No hardcoded spacing in component Rust code"
    - "Hover states use design system transitions"
  artifacts:
    - path: "crates/iou-frontend/assets/style.css"
      provides: "3D control CSS classes using design system variables"
      contains: ".filter-panel-3d", ".view-toggle-container", ".density-heatmap-container", ".btn-view-toggle", ".btn-clear-filters"
      min_lines: 100
    - path: "crates/iou-frontend/src/components/view_toggle.rs"
      provides: "ViewToggle with CSS classes instead of inline styles"
      contains: "no inline styles on button, uses CSS classes"
    - path: "crates/iou-frontend/src/components/filter_panel_3d.rs"
      provides: "FilterPanel3D with CSS classes"
      contains: "no hardcoded colors, uses var(--flevoland-blauw)"
    - path: "crates/iou-frontend/src/components/density_heatmap.rs"
      provides: "DensityHeatmap with CSS classes"
      contains: "no hardcoded colors"
  key_links:
    - from: "view_toggle.rs, filter_panel_3d.rs, density_heatmap.rs"
      to: "style.css"
      via: "class attributes referencing CSS classes"
      pattern: "class: \"btn-view-toggle\""
    - from: "MapLibre easeTo"
      to: "CSS transitions"
      via: "matching timing (500ms camera, 300ms UI)"
      pattern: "easeTo.*duration.*500"
---

<objective>
Add smooth CSS transitions for view toggles and apply design system styling to all 3D control components using CSS variables from style.css.

Purpose: View transitions feel polished with synchronized animations (500ms camera, 300ms UI), and all controls match the Provincie Flevoland design system for visual consistency.

Output: CSS classes for 3D controls, inline styles removed from components, smooth transitions on state changes.
</objective>

<execution_context>
@/Users/marc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marc/.claude/get-shit-done/templates/summary.md

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/2.4-polish/2.4-CONTEXT.md
@.planning/phases/2.4-polish/2.4-RESEARCH.md

@crates/iou-frontend/assets/style.css
@crates/iou-frontend/src/components/view_toggle.rs
@crates/iou-frontend/src/components/filter_panel_3d.rs
@crates/iou-frontend/src/components/density_heatmap.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3D control CSS classes to style.css</name>
  <files>crates/iou-frontend/assets/style.css</files>
  <action>
    Add to `crates/iou-frontend/assets/style.css` at the end (before any existing 3D-specific styles):

    CSS sections to add:
    - `.btn-3d-control` - Common control button style with CSS variables
    - `.view-toggle-container` - Position and fadeInDown animation (0.4s)
    - `.view-toggle-content` - Flex layout for toggle button + label
    - `.view-toggle-label` - Small text styling
    - `.density-heatmap-container` - Position and fadeInUp animation (0.4s)
    - `.density-heatmap-content` - Flex layout
    - `.density-heatmap-label` - Small text styling
    - `.filter-panel-3d` - Panel styling with fadeInRight animation (0.4s)
    - `.filter-header`, `.filter-group`, `.filter-label` - Typography and spacing
    - `.filter-slider-row`, `.filter-slider` - Slider styling with webkit/moz thumb styles
    - `.btn-clear-filters` - Clear button styling
    - `.building-count-display` - Count indicator styling

    All styling should use:
    - `var(--flevoland-blauw)` for primary color
    - `var(--spacing-*)` for spacing
    - `var(--radius-*)` for border radius
    - `var(--shadow-*)` for shadows
    - `var(--surface)` for backgrounds
    - `var(--text-primary)` and `var(--text-secondary)` for text

    Transitions: 0.2s ease for hover states, 0.3s ease for opacity changes
    Animations: fadeInDown, fadeInUp, fadeInRight at 0.4s ease-out
  </action>
  <verify>
    <automated>grep -n "filter-panel-3d\|view-toggle-container\|density-heatmap-container" crates/iou-frontend/assets/style.css</automated>
  </verify>
  <done>CSS classes added to style.css for all 3D controls with design system variables</done>
</task>

<task type="auto">
  <name>Task 2: Remove inline styles from ViewToggle component</name>
  <files>crates/iou-frontend/src/components/view_toggle.rs</files>
  <action>
    Modify `crates/iou-frontend/src/components/view_toggle.rs`:

    1. Update ViewToggle component (lines 134-159) to use CSS classes:
       - Container: `class: "view-toggle-container"`
       - Button: `class: "btn-3d-control"` (add "active" class when 3D mode)
       - Label: `class: "view-toggle-label"`

    2. Remove all inline `style:` attributes:
       - Container positioning
       - Button styling (background, padding, border, etc.)
       - Label styling (margin-top, font-size, color)

    The positioning is handled by the CSS class which maintains the same absolute position.
  </action>
  <verify>
    <automated>grep -c 'style: "' crates/iou-frontend/src/components/view_toggle.rs | grep -q "0" && echo "PASS: No inline styles found" || echo "FAIL: Inline styles still present"</automated>
  </verify>
  <done>ViewToggle uses CSS classes, no inline styles</done>
</task>

<task type="auto">
  <name>Task 3: Verify FilterPanel3D uses CSS classes</name>
  <files>crates/iou-frontend/src/components/filter_panel_3d.rs</files>
  <action>
    Review `crates/iou-frontend/src/components/filter_panel_3d.rs`:
    - Verify component uses CSS classes: `filter-panel-3d`, `filter-header`, `filter-group`, `filter-label`, `filter-slider-row`, `filter-slider`, `btn-clear-filters`
    - Verify no hardcoded colors exist in class attributes
    - Verify no inline `style:` attributes exist
    - If any issues found, fix them (component already uses classes correctly)
  </action>
  <verify>
    <automated>grep -n 'style: "' crates/iou-frontend/src/components/filter_panel_3d.rs || echo "PASS: No inline styles"</automated>
  </verify>
  <done>FilterPanel3D verified to use CSS classes, no inline styles</done>
</task>

<task type="auto">
  <name>Task 4: Remove inline styles from DensityHeatmap component</name>
  <files>crates/iou-frontend/src/components/density_heatmap.rs</files>
  <action>
    Modify `crates/iou-frontend/src/components/density_heatmap.rs`:

    1. Update DensityHeatmap component (lines 430-463) to use CSS classes:
       - Container: `class: "density-heatmap-container"`
       - Button: `class: "btn-3d-control"` (add "active" class when enabled)
       - Label: `class: "density-heatmap-label"`

    2. Remove all inline `style:` attributes:
       - Container positioning
       - Button styling
       - Label styling
  </action>
  <verify>
    <automated>grep -c 'style: "' crates/iou-frontend/src/components/density_heatmap.rs | grep -q "0" && echo "PASS: No inline styles found" || echo "FAIL: Inline styles still present"</automated>
  </verify>
  <done>DensityHeatmap uses CSS classes, no inline styles</done>
</task>

</tasks>

<verification>
After code changes, verify in browser:
1. Open page with 3D map enabled
2. Click 2D/3D toggle - observe smooth camera animation (500ms) and UI transition (300ms)
3. Change filter sliders - observe smooth slider movement, no jank
4. Hover over buttons - observe smooth color transitions (0.2s)
5. Check all controls use consistent colors (Provincie Flevoland blue)
6. Verify no visual inconsistencies between controls
</verification>

<success_criteria>
- All 3D controls use CSS classes from style.css
- No hardcoded colors in component Rust code
- Smooth transitions on hover states (0.2s)
- Smooth animations on mount (0.4s fadeIn)
- View toggle has synchronized animation (camera 500ms, UI 300ms)
- All controls match design system styling
</success_criteria>

<output>
After completion, create `.planning/phases/2.4-polish/2.4-02-SUMMARY.md`
</output>
