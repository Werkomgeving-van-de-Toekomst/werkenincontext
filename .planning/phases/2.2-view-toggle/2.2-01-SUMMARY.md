---
phase: 2.2-view-toggle
plan: 01
subsystem: ui
tags: [dioxus, maplibre, view-toggle, 2d-3d, fill-extrusion, local-storage]

# Dependency graph
requires:
  - phase: 2.1-building-filtering
    provides: building-3d layer with FilterPanel3D integration
provides:
  - ViewToggle state module with ViewMode enum (TwoD/ThreeD)
  - ViewToggle component with setPaintProperty single-layer architecture
  - JavaScript bridge for 2D/3D switching with localStorage persistence
affects: [2.3-heatmap-density, 2.4-export-sharing]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single-layer architecture via map.setPaintProperty() for 2D/3D switching"
    - "localStorage persistence using 'viewMode' key"
    - "eval() interop for JavaScript bridge functions"

key-files:
  created:
    - crates/iou-frontend/src/state/view_toggle.rs
    - crates/iou-frontend/src/components/view_toggle.rs
  modified:
    - crates/iou-frontend/src/state/mod.rs
    - crates/iou-frontend/src/components/mod.rs
    - crates/iou-frontend/src/pages/data_verkenner.rs

key-decisions:
  - "Single-layer architecture (setPaintProperty) chosen over dual-layer to prevent state desync (PITFALL-03)"
  - "Default to 3D mode to match initial map state with pitch=60"
  - "Dutch button labels: '3D Weergave' / '2D Weergave'"

patterns-established:
  - "View mode switching via fill-extrusion-height: 0 for 2D, ['coalesce', ['get', 'height'], 10] for 3D"
  - "Pitch adjustment: 0 for 2D (top-down), 60 for 3D (perspective)"
  - "TDD approach: state tests + component tests = 14 total passing tests"

requirements-completed: [VIEW-01, VIEW-02, VIEW-03]

# Metrics
duration: 2min
completed: 2026-03-08
---

# Phase 2.2 Plan 1: View Toggle Summary

**2D/3D view toggle using single-layer fill-extrusion height switching with localStorage persistence**

## Performance

- **Duration:** ~2 minutes
- **Started:** 2026-03-08T08:59:31Z
- **Completed:** 2026-03-08T09:01:20Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- ViewToggle state module with ViewMode enum (TwoD/ThreeD) and ViewToggle struct wrapper
- ViewToggle component with JavaScript bridge using single-layer setPaintProperty architecture
- Integration into DataVerkenner page at top-left of map when 3D is enabled
- localStorage persistence using 'viewMode' key
- 14 unit tests passing (7 state tests + 7 component tests)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create ViewToggle state module** - `16bd8cb` (feat)
2. **Task 2: Create ViewToggle component with JavaScript bridge** - `d9fae34` (feat)
3. **Task 3: Integrate ViewToggle into DataVerkenner page** - `72350e2` (feat)

**Plan metadata:** TBD (docs commit)

## Files Created/Modified

- `crates/iou-frontend/src/state/view_toggle.rs` (new) - ViewMode enum with toggle/is_3d/label/button_label methods, ViewToggle wrapper struct
- `crates/iou-frontend/src/components/view_toggle.rs` (new) - build_set_view_mode_script(), build_get_initial_view_script(), ViewToggle component
- `crates/iou-frontend/src/state/mod.rs` - Added view_toggle module and exports
- `crates/iou-frontend/src/components/mod.rs` - Added view_toggle module and exports
- `crates/iou-frontend/src/pages/data_verkenner.rs` - Imported ViewToggle and added to map panel

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None - all tests passed, cargo check succeeded.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Single-layer architecture established for 2D/3D switching
- ViewToggle component positioned at top-left of map (z-index: 1000)
- FilterPanel3D adjusted to top: 60px to avoid overlap
- Ready for Phase 2.3: Heatmap/Density visualization

---
*Phase: 2.2-view-toggle*
*Completed: 2026-03-08*
