# Section 04: Map3D Component - Diff

```diff
diff --git a/crates/iou-frontend/src/components/map_3d.rs b/crates/iou-frontend/src/components/map_3d.rs
index fb2e51e..f2491d8 100644
--- a/crates/iou-frontend/src/components/map_3d.rs
+++ b/crates/iou-frontend/src/components/map_3d.rs
@@ -5,6 +5,7 @@
 //! support (pitch, rotate, zoom).

 use std::env;
+use dioxus::prelude::*;

 /// Configuration for the MapLibre GL JS 3D map instance.
 ///
@@ -344,4 +345,240 @@ mod config_tests {
     }
 }

-// Placeholder - Map3D component implementation in section-04-map3d-component
+// ============================================================================
+// Map3D Component - Dioxus wrapper for MapLibre GL JS
+// ============================================================================
+
+/// Event types emitted by the Map3D component.
+///
+/// These events are generated by user interactions with the map
+/// and can be handled by parent components.
+#[derive(Debug, Clone, PartialEq)]
+pub enum MapEvent {
+    /// Map has finished loading the initial style and sources
+    Loaded,
+    /// Map started loading a new style or source
+    LoadStart,
+    /// Map finished loading a new style or source
+    LoadEnd,
+    /// An error occurred during map loading
+    Error { message: String },
+    /// User moved the map
+    Move { zoom: f64, center: (f64, f64) },
+    /// User zoomed the map
+    Zoom { zoom: f64 },
+    /// User pitched/tilted the map
+    Pitch { pitch: f64 },
+    /// User rotated the map
+    Rotate { bearing: f64 },
+    /// User clicked on the map
+    Click { lng: f64, lat: f64 },
+}
+
+impl MapEvent {
+    /// Convert the event to a JSON string for JavaScript.
+    pub fn to_json(&self) -> String {
+        match self {
+            MapEvent::Loaded => r#"{"Loaded":null}"#.to_string(),
+            MapEvent::LoadStart => r#"{"LoadStart":null}"#.to_string(),
+            MapEvent::LoadEnd => r#"{"LoadEnd":null}"#.to_string(),
+            MapEvent::Error { message } => format!(r#"{{"Error":"{}"}}"#, message),
+            MapEvent::Move { zoom, center } => {
+                format!(r#"{{"Move":{{"zoom":{},"center":[{},{}]}}}}"#, zoom, center.0, center.1)
+            }
+            MapEvent::Zoom { zoom } => format!(r#"{{"Zoom":{{"zoom":{}}}}}"#, zoom),
+            MapEvent::Pitch { pitch } => format!(r#"{{"Pitch":{{"pitch":{}}}}}"#, pitch),
+            MapEvent::Rotate { bearing } => format!(r#"{{"Rotate":{{"bearing":{}}}}}"#, bearing),
+            MapEvent::Click { lng, lat } => format!(r#"{{"Click":[{},{}]}}"#, lng, lat),
+        }
+    }
+}
+
+/// Builds the JavaScript initialization script for the map.
+///
+/// This script:
+/// 1. Checks for WebGL2 support
+/// 2. Creates the MapLibre map instance
+/// 3. Configures initial view state
+/// 4. Sets up event listeners
+pub fn build_map_init_script(config: &Map3DConfig) -> String;
+    format!(r#"
+        (function() {{
+            const container = document.getElementById('{}');
+            if (!container) {{
+                console.error('Map container not found: {}');
+                return;
+            }}
+
+            if (window['map_{}']) {{
+                console.log('Map already initialized: {}');
+                return;
+            }}
+
+            const map = new maplibregl.Map({{
+                container: '{}',
+                style: 'https://api.maptiler.com/maps/streets/style.json?key=YOUR_KEY',
+                center: [{}, {}],
+                zoom: {},
+                pitch: {},
+                bearing: {},
+                minZoom: {},
+                maxZoom: {},
+                antialias: true
+            }});